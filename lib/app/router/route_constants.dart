/// Central constants for app route paths.
/// Use these constants throughout the app to reference routes consistently
/// and avoid hardcoding path strings.
abstract class RouteConstants {
  /// Funds page route path.
  static const String funds = '/funds';

  /// Transaction history page route path.
  static const String history = '/history';
}
